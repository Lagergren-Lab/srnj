# SCONCE2 paths (defaults to external/sconce2/ in project root if not specified)
# Uncomment and modify these lines if you have SCONCE2 installed elsewhere:
# SCONCE2_EXEC: "/path/to/your/sconce2/sconce2"
# SCONCE2_SCRIPTS: "/path/to/your/sconce2/scripts"

RESULTS_DIR: "../../../output/snakemake_results_demo/" # change this if you want results in a different directory
REPEATS: 2
N_CELLS: [15, 25]
SCONCE2_K: 5
